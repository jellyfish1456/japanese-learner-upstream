// Placeholder — will be replaced by the full generated file
export interface SpotDetail {
  name: string;
  desc: string;
  tips: string;
  tags: string[];
}

export const spotDetails: Record<string, SpotDetail[]> = {};
